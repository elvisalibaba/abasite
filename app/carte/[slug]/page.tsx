import {notFound} from "next/navigation";
import {createClient} from "@supabase/supabase-js";
import "./profile.css";
export const dynamic="force-dynamic";
export const metadata={title:"Profil professionnel | ABA"};
export default async function BusinessProfilePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)notFound();const supabase=createClient(url!,key!,{auth:{persistSession:false}}),{data}=await supabase.from("card_requests").select("first_name,middle_name,last_name,job_title,department,phone,email,bio,linkedin_url,personal_website").eq("public_slug",slug).eq("card_type","visite").in("status",["approuvee","imprimee"]).maybeSingle();if(!data)notFound();const name=`${data.first_name} ${data.middle_name} ${data.last_name}`.replace(/\s+/g," ").trim();return <main className="person-portfolio"><section className="portfolio-hero"><img src="/image.png" alt="Africa Business Agency"/><span>PROFIL PROFESSIONNEL VÉRIFIÉ</span><h1>{name}</h1><h2>{data.job_title}</h2><p>{data.department}</p></section><section className="portfolio-content"><div className="portfolio-about"><small>À PROPOS</small><h3>Expertise professionnelle</h3><p>{data.bio||`${name} exerce la fonction de ${data.job_title} au sein de ${data.department||"Africa Business Agency"}.`}</p></div><aside><a href={`tel:${data.phone}`}>Téléphone<strong>{data.phone}</strong></a><a href={`mailto:${data.email}`}>E-mail<strong>{data.email}</strong></a>{data.linkedin_url&&<a href={data.linkedin_url} target="_blank" rel="noreferrer">LinkedIn<strong>Voir le profil ↗</strong></a>}{data.personal_website&&<a href={data.personal_website} target="_blank" rel="noreferrer">Site personnel<strong>Visiter ↗</strong></a>}</aside></section><footer><span>Profil associé à une carte professionnelle ABA validée</span><a href="https://aba.cd">aba.cd</a></footer></main>}
